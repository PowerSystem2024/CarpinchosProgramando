// Retornamos un valor de la funcion

function miFuncion(a, b){
    return a + b;
}

let resultado = miFuncion(6, 7);    // Creamos una variable a la que le asignamos el resultado de la funcion, lo que retorno.
console.log(resultado);