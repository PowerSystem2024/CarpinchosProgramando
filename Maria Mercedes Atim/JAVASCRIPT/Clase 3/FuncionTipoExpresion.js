// Declaramos una funcion tipo expresión o funcion anónima
let x = function(a, b){return a + b}; //Se cierra con ;
let resultado = x(5, 6); //Al llamarla se indica la variable con paréntesis obviando la palabra function
console.log(resultado);